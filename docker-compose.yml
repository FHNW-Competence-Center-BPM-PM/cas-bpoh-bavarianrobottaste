services:
  web:
    build:
      context: .
    container_name: bavarian-robotaste-web
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
